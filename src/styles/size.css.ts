import { theme } from "@/styles/theme-default";

export const width = theme.width;

export const height = theme.height(theme.spacing);

export const space = theme.spacing;

export const position = theme.inset(theme.spacing);
