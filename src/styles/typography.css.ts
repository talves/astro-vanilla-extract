import { theme } from "@/styles/theme-default";

export const fontFamily = {
  "sans-serif": theme.fontFamily.sans.toString(),
  serif: theme.fontFamily.serif.toString(),
  mono: theme.fontFamily.mono.toString(),
};

export const fontWeight = theme.fontWeight;

export const lineHeight = theme.lineHeight;

export const fontSize = theme.fontSize;
